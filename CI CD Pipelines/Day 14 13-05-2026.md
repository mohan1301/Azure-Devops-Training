Date: 13-05-2026
Agenda for today

SonarQube contains:
Security field looks after Vulnerabilities, certificates harcoding,
Reliability - Bugs
Maintainability - Unused variables, Long logic
Coverage - Does all the tests are covered in the test cases
Duplications - Duplicate lines or functions

Even after the SonarQubr failed in the logs, it still showed as a Green Build. So, we have to install a plugin from marketplace of Azure DevOps.

In the process of adding the gate in the pipeline, we will be asked for Project Key. That needs to be added explicitly. In the dropdown, we cannot see the Project key of Sonarqube due to some issues.
Get the Project key from Project Information from SonarQube portal

Sequence of pipeline

Today's class is all about Adding the SonarQube stages in the pipeline both in Java and Dotnet porject and run.

We can add the variables in Library section which is visible on the left

ACR(Container Registry)/AKS(Kubernetes Solution)

