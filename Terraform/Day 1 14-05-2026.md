Date: 14-05-2026
Agenda for today

In order to host a Python project, we need to host in Docker

We should still cover Variables, Keyvaults, Test plansm Artifacts in Azure DevOps
But for today, we will cover Terraform

Infra build tool -> VM Ware, HyperV, Kubernetes

Opensource IAC Tool --> managed by Hashicorp
Terraform is used for Provision of new infra, Manage and versioning of infra
Able to manage multiple Cloud providers

Imperative -> For a single command, if the whole resources are created.
Declarative configuration language

json -> Hashicorp config language(hcl)

Azure -> ARM, Bicep templates
AWS -> Cloud Formation

So, to avoid multiple formats. we have introduced Terraform.
Universal IAC Tool --> Terraform
1. Multicloud support
2. Open Source and free
3. Can be part o CI/CD ecosystem -- > Git, Github, Azure pipeline, jenkins
4. Easy to learn and use

Terraform Work flow
Meta argments - for each, ignore
Providers I
Modules
Resource
Terraform lifecycle commands - create, delete
Terraform state file - Unique thing when compared to others
Terraform Backend
Outputs
tfvars
variables
Drift Detection
Execution plan


Imperative way - For Basic VNET -> azcli create resource vnet "vnet name" cidr "10.0.0.0/16"  which is a single command

Declarative way - We will write a dedicated resource block.
resource{
    azurerm_resource_vnet "azurevnet"
    cidr = "10.0.0.0/16"
}

Configuration Drift: Cloud Formation, Bicep Templates, ARM doesnt have the capability to identify the drift which becomes a unique thing for Terraform

Terraform Workflow
init -> validate -> plan -> apply -> destroy

Terraform fmt is for formatting the config files.
azure cli is a command tool for both Bicep and Terraform
terraform extension for auto text while writing commands
tf scripts

Create +
Destroy -
No changes =
Inplace update ~
destroy and create -/+

Terraform .tf files are not whitespace sensitive. It means, even though there are whitespaces... it doesnt fail at all...

NOTE: Bicep templates doesnt have an option to destroy resources
