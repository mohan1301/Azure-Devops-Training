Date: 03-06-2026
Agenda for today

AKS

For pods, we have a concept called label to identify for which activity this pod will be using

As long as the Pod exists, pod IP wont be changed

Service IP - It can be either Cluster IP, Nodeport IP, Loadbalancer IP

Docker file vs Docker compose file
In order to start multiple containers at a time... docker compose file will be used


CMD vs Entrypoint in Docker file
Entrypoint means, if it is a fixed one.