Date: 10-06-2026
Agenda for today

Deployments in Declarative way

Versioning, track, modify, update
Desired state
Actual state

1. Key value pair
dev: 2 cpus
Here, key means dev... value means 2 cpus

2. List example - 
men:
  - shirts
  - tshirts
  - shoes

3. Nested List
server configuration:
    hardware config:
        -name
        -no of cpus
        -ram
software config:
    -osfamily
    -apache version
    -mysqlversion

If we are specifying three lines(---), new start of the file

In Kubernetes, we call the deployment files as Manifest file
1. First 2 lines - apiversion and specifications will be mentioned
what you want to create: apiversion ? the answer can be any of the below
    1. deployments
    2. pod
    3. Replica set
    4. daemonset
    5. job
    6. cronjob
    7. secrets
    8. configmaps
    9. service
    10. Ingress -  
2. metadata - What is the name of the manifest - Name of the pods
3. Specifications - 
    1. replicas
    2. label
    3. image
    4. port

now we will be creating deployment.yml and service.yml in screen share


matchLabels - We are just matching labels.. not defining labels
selector:
    matchLabels:
        app: my-app


metadata:
  name: nginx-service
This is the name of the service

metadata: 
  name: webnginx
This is the name of the deloyment

For external server to Node, we use 32319 port 
For Node to Container communication, we use containerPort: 80 because nginx uses port number 80

Sequence of execution
first, deploment file will be run and it creates 3 pods and adds a labels... and then service.yml will be run
once the service is created, it will route the traffic to matchlabels pods

kubectl apply -f service.yml ---> Here, -f means file name
kubectl apply -f deployment.yml ---> Here, we are tryign  

template is used for what reason we are using this deployment file

For Node to Container communication, we use containerPort: 80 because nginx uses port number 80




